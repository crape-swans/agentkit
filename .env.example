# Copy to .env before running locally
# cp .env.example .env

# Ethereum RPC URL (local or remote)
RPC_URL=http://localhost:8545

# Network name (e.g., mainnet, testnet, custom)
NETWORK=development

# Optional API key for services (if required)
API_KEY=

# Log level for debugging (info | debug | warn)
LOG_LEVEL=info
